# Smart File Organizer uses only the Python standard library:
# pathlib, os, shutil, json, datetime, logging, tkinter, unittest.
#
# No external (pip-installed) packages are required.
# This file is kept for completeness / professionalism, and so that
# `pip install -r requirements.txt` works and does nothing harmful.
